<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create(config('cookie-consent.tables.scripts', 'consent_scripts'), function (Blueprint $table): void {
            $table->id();
            $table->string('name');
            $table->string('provider')->nullable();
            $table->string('category', 20)->index();
            $table->string('position', 10)->default('head');
            $table->string('kind', 10)->default('external');
            $table->string('src', 2048)->nullable();
            $table->text('code')->nullable();
            $table->json('script_attributes')->nullable();
            $table->json('cookies')->nullable();
            $table->unsignedInteger('sort_order')->default(0);
            $table->boolean('is_active')->default(true);
            $table->timestamps();
        });
    }
};
